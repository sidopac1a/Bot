{
  "name": "wc-bot",
  "version": "1.2.0",
  "main": "main.js",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "electron-dev": "concurrently \"npm:start\" \"wait-on http://localhost:3000 && electron .\"",
    "electron-build": "npm run build && electron ."
  },
  "devDependencies": {
    "electron": "^25.0.0",
    "wait-on": "^7.0.1",
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "axios": "^1.5.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-hot-toast": "^2.4.0",
    "socket.io-client": "^4.6.1",
    "react-icons": "^4.11.0"
  },
  "build": {
    "appId": "com.sidoDz.wcbot",
    "productName": "WCBot 1.2 v by sidoDz",
    "win": {
      "target": "nsis",
      "icon": "icon.png"
    }
  }
}
